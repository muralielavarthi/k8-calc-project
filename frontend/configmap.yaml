apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: calc-namespace
  labels:
    project: calc
    division: finance
data:
  nginx.conf: |
    events {}

    http {
        server {
            listen 80;

            location / {
                root /usr/share/nginx/html;
                index index.html;
            }

            location /api/ {
                proxy_pass http://backend:8080/;
            }
        }
    }
